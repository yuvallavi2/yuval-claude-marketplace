# Deprecated Sections

This file is the sanctioned path for removing sections from CLAUDE.md on re-run. The additive merge itself still never deletes content — but Step 5.5 of the init skill reads this file and removes any listed sections from the existing CLAUDE.md *before* the additive merge applies.

Each entry: section heading (exact `## ` form), a reason, and the date it was deprecated. Do not delete entries — they document the template's evolution.

## Currently deprecated

- `## Session Logging` — superseded by `## Session Continuity` (2026-04-19). The Continuity section covers logging plus the session-start/session-end contract.
- `## Wiki Discipline` — moved to `/wiki/memory-protocol.md` (2026-04-27). Per D-018, the memory protocol now lives in the wiki, not in CLAUDE.md. CLAUDE.md keeps only the `## Long-term memory protocol` pointer.
- `## Session Continuity` — moved to `/wiki/memory-protocol.md` (2026-04-27). Per D-018, the memory protocol now lives in the wiki, not in CLAUDE.md. CLAUDE.md keeps only the `## Long-term memory protocol` pointer.
